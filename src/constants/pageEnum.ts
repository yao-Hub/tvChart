export const enum PageEnum {
  LOGIN = "/login",
  CHART = "/chart",
  LOGIN_HOME = "/login/home",
  LOGIN_OPT = "/login/otp",
  LOGIN_ACCOUNTS = "/login/accounts",
  LOGIN_FORGET_PASSWORD = "/login/forgetPassword/:server",
  LOGIN_FORGET_ACCOUNT = "/login/forgetAccount/:server",
  LOGIN_REGISTER = "/login/register/:server",
  PROTOCOL = "/protocol",
}
